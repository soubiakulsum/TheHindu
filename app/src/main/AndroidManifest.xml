<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thehinduclone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheHinduClone">
        <activity android:name=".kotlin.ui.PremiumActivity"></activity>
        <activity android:name=".kotlin.ui.TrendingActivity" />
        <activity android:name=".kotlin.ui.BriefingActivity" />
        <activity android:name=".SearchBarActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".ReadLaterActivity" />
        <activity
            android:name=".ui.home.SideNavActivity"
            android:label="@string/title_activity_side_nav"
            android:theme="@style/Theme.TheHinduClone.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>